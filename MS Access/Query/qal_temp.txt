SELECT TOP 60 
  id, 
  isActive, 
  FORMAT(dateTemp,"mm/dd/yyyy") AS [Date], 
  FORMAT(dateTemp,"HH:nn:ss") AS [Time], 
  temp &' ' & tempUnit AS Temperature
FROM tdu_temp
WHERE isActive = "Yes"   
AND dateTemp >= DATEADD("d",-60,NOW())
ORDER BY dateTemp DESC;
