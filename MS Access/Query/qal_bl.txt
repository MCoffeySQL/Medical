SELECT 
  id, 
  isActive, 
  FORMAT(dateBP,"mm/dd/yyyy") AS [Date], 
  FORMAT(dateBP,"hh:nn:ss") AS [Time], 
  [Position], 
  systolic & "/" & diastolic AS BP, 
  Pulse
FROM tdu_bp
WHERE isActive = 'Yes'     
  AND dateBP >= DATEADD('d',-60,NOW())
ORDER BY dateBP DESC;
